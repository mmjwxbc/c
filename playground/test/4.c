#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>

#define STUDENT_FILE	"/usr/bin/student_data"

int main()
{
	struct student	current_record;
	int 		studentfd;
	int		reclen = sizeof(current_record);

	if((

print studentp->first, studentp->last, studentp->gpa
